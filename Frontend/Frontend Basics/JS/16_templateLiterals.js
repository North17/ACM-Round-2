// template literals
// We enclose js code within ${ }

const person = {
    "name": "Marc Stanley",
    "age": 18
};

const temp = `Hey my name is ${person.name}!
I am ${person.age} years old!`;

console.log(temp);

